[pytest]
# Make the risk-scoring-engine root a source root so that
# `from app.models import ...` resolves correctly in both
# local runs and CI (without needing PYTHONPATH to be set manually).
pythonpath = .
testpaths = tests
